// Inherit the parent event
event_inherited();

//to set variables

spd = 0; //max speed

current_speed = 0; //speed the being is currently walking at

acceleration = 0; //delay between walk/stand switch

max_jump_spd = 0; //max jump height

jump_spd = max_jump_spd; //speed the being is jumping at (if it's jumping)

